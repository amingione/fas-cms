# dotenvx — REMOVED

dotenvx was removed from this project on 2026-04-22.

Secrets are now managed as plaintext env vars in the **Netlify dashboard** (production)
and in `.env.local` (local dev, gitignored).

This directory can be deleted.
