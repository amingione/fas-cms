---
import BaseLayout from '@/layouts/BaseLayout.astro';
const studioUrl = import.meta.env.PUBLIC_STUDIO_URL || '';
---
<BaseLayout>
  <div class="container mx-auto px-4 py-10 text-white" style="min-height:70vh">
    <h1 class="text-2xl md:text-3xl font-bold mb-4">Content Studio</h1>
    {studioUrl ? (
      <iframe
        src={studioUrl}
        class="w-full h-[75vh] rounded-lg border border-white/20 bg-black"
        sandbox="allow-scripts allow-same-origin allow-forms allow-popups"
      />
    ) : (
      <div class="max-w-2xl text-white/80">
        <p class="mb-3">Set <code class="px-1 rounded bg-white/10">PUBLIC_STUDIO_URL</code> to an externally hosted Sanity Studio to embed it here.</p>
        <p class="mb-3">Example: <code class="px-1 rounded bg-white/10">https://your-project-id.sanity.studio</code></p>
        <p class="mb-3">Alternatively, we can mount a local Studio app in this repo if you prefer.</p>
      </div>
    )}
  </div>
</BaseLayout>

