'use client';

import HeaderAuth from './HeaderAuth';

/**
 * Thin wrapper to mount the header auth React helper on the client.
 */
export default function HeaderAuthLoader() {
  return <HeaderAuth />;
}
