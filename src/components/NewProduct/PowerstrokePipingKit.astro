<section>
  <div class="banner-container">
    <a
      href="https://www.fasmotorsports.com/shop/2020-6-7l-powerstroke-piping-kit"
      class="product-drop-badge"
    >
      <div class="badge-title">NEW Product Drop →</div>
      <div class="badge-sub">2020+ 6.7L Powerstroke Piping Kit</div>
    </a>
    <img
      src="/images/products/6.7l-product-drop.png"
      alt="Powerstroke Piping Kit"
      width="1920"
      height="1080"
      loading="lazy"
      decoding="async"
      class="banner-image"
    />
  </div>

  <style>
    .banner-container {
      width: 100%;
      overflow: hidden;
    }

    .banner-image {
      width: 100%;
      height: auto;
      display: block;
      object-fit: cover;
      object-position: center;
    }

    @media (max-width: 768px) {
      .banner-image {
        object-fit: contain;
      }
      .product-drop-badge {
        top: 10px;
        right: 0px;
        padding: 8px 12px;
        border-radius: 6px;
        transform: scale(0.55);
      }

      .badge-title {
        font-size: 0.8rem;
      }

      .badge-sub {
        font-size: 0.7rem;
      }
    }

    .product-drop-badge {
      position: absolute;
      top: 20px;
      right: 20px;
      background: rgba(0, 0, 0, 0.65);
      padding: 12px 18px;
      border-radius: 8px;
      text-decoration: none;
      display: flex;
      flex-direction: column;
      backdrop-filter: blur(6px);
    }

    .badge-title {
      color: #00e0ff;
      font-size: 0.95rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .badge-sub {
      color: white;
      font-size: 0.85rem;
      margin-top: 4px;
      font-weight: 500;
    }

    .banner-container {
      position: relative;
    }
  </style>
</section>
