import { Filter } from 'lucide-react';

// Named export for explicit imports
export { Filter as FilterIcon };

// Default export for convenience: import FilterIcon from '@components/icons/FilterIcon'
export default Filter;

